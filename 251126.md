# Arbeitsbericht am 26.11.2025

- Name: Armen Jalilyan
- Klasse: 2 AHITS
- Datum: 26.11.2025
- Fach: ITS1 Gruppe 1
- Thema stdin/stdout


# Übung 2.5 - (c in/out)
## Aufgabenstellung:
- Auch selbstgeschriebene C Programme lesen von stdin und schreiben auf stdout. Und daher kann man auch hier die Dateiumleitung mit <, >, oder >> verwenden. Das Programm merkt davon gar nichts.

- Erstelle eine Datei sumieren.cpp und öffne diese im Replit Editor.

- Schreibe ein C Programm das zuerst eine Anzahl (int n) einliest.

- Dann werden n weitere ganze Zahlen eingelesen und die Summe dieser Zahlen ermittelt.

- Am Ende gibt das Programm die Summe aus (dies soll die einzige Ausgabe des Programms sein).
Beispiel-Programmablauf (n=3):
```sh
3
5
4
2
11
```
- Bedeutung: Es sollen 3 Zahlen eingegeben werden, diese sind 5, 4 und 2. Deren Summe ist die Ausgabe 11.

- Compiliere das Programm mit folgender Kommandozeile:
g++ -o summiere summiere.cpp
Wenn keine Compile-Fehler auftreten wird im gleichen Verzeichnis das Programm mit dem Namen sum erzeugt. Prüfe mit ls ob vorhanden.

- Behebe evtl. Compiler-Fehler und starte dann das Programm mit:
./summiere
Erstelle eine Textdatei mit den Input Daten für das Programm sum. Z.B.
```sh
5
1
2
3
4
5
```
Rufe nun das Programm auf und leite die Textdatei per Dateiumleitung ins stdin des Programms summiere um. Schreibe weiters den Output des Programms in eine Textdatei.

- Lösung (summieren.cpp):
```sh
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int sum = 0;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        sum += x;
    }

    cout << sum;
    return 0;
}
```
- Compilieren:
```sh
g++ -o summiere summieren.cpp
```

- Input-Datei:
```sh
5
1
2
3
4
5
```
- Programm mit Umleitung starten:
```sh
./summiere < in.txt > out.txt
```
## Erklärung:
- Das Programm liest zuerst die Anzahl der Zahlen.
-	Dann addiert es diese Zahlen und gibt nur die Summe aus.

# Übung 2.6 - C Programm mit EOF
## Aufgabenstellung:
- Schreibe das vorhergehende Programm so um damit die Anzahl als erster Wert nicht mehr notwendig ist, d.h. das Ende der Daten automatisch erkannt wird.

- Hinweis: Es gibt das Konzept des End of File (EOF). Ist stdin die Tastatur so ist EOF Ctrl+D. Der Befehl scanf bietet eine Möglichkeit die EOF Situation abzufragen.

- Lösung (summieren_eof.cpp):
```sh
#include <iostream>
using namespace std;

int main() {
    int x;
    int sum = 0;

    while (cin >> x) {
        sum += x;
    }

    cout << sum;
    return 0;
}
```
- Compilieren:
```sh
g++ -o summiere_eof summieren_eof.cpp
```

- Programm starten:
```sh
./summiere_eof < in.txt > out2.txt
```
# Übung 2.7 (C Hallo Welt)
## Aufgabenstellung:
Erstelle nur mit Hilfe von echo Befehlen eine Datei hello.cpp mit folgendem Inhalt:
```sh
#include <iostream>

int main() {
  printf("\n\t*** Hallo Welt ***\n");
  return 0;
}
```
Hinweis: Verwende einfache Hochkommas rund um den mit echo auszugebenden Text (Beispiel: echo 'text') ansonsten hat die shell Schwierigkeiten mit den Sonderzeichen #,<,> etc.

Compiliere und starte das C Programm aus der Kommandozeile.
- Terminal:
```sh
echo '#include <iostream>' > hello.cpp
echo '' >> hello.cpp
echo 'int main() {' >> hello.cpp
echo '  printf("\n\t*** Hallo Welt ***\n");' >> hello.cpp
echo '  return 0;' >> hello.cpp
echo '}' >> hello.cpp
```
- Compilieren und Starten:
```sh
g++ -o hello hello.cpp
./hello
```
# Übung 2.8 (semikolon in command-line)
## Aufgabenstellung:
Mit Strich-Punkt (;) getrennt lassen sich auch 2 Befehle hintereinander in der Kommandozeile schreiben – diese werden dann nacheinander ausgeführt.

Probiere aus – in einer einzigen Kommandozeile:

- Mit echo einen Text in eine Datei schreiben und deren Inhalt gleich wieder ausgeben
- Ein C-Programm compilieren und sofort danach starten

- Lösung:
```sh
echo "Hallo" > datei.txt ; cat datei.txt
```
```sh
g++ -o hello hello.cpp ; ./hello
```
# Übung 2.9 (Here-Documents)

## Aufgabenstellung:
Informiere dich über die Here-Documents Schreibweise (=Syntax) und führe damit die Aufgabenstellungen “Textdatei erstellen” und “C Hallo Welt” noch einmal aus.

- Textdatei:
```sh
cat <<EOF > made_by_here.txt

=====================

=    HTL BRAUNAU    =

=====================

= 2AHITS Gruppe n   =

= x Schülerinnen    =

= y Schüler         =

=====================
EOF

```
- Hello.cpp
```sh
cat <<EOF > hello.cpp
#include <iostream>

int main() {
  printf("\n\t*** Hallo Welt ***\n");
  return 0;
}
EOF
```