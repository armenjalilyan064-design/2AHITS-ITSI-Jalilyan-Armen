# Arbeitsbericht am 20.05.2026

- Name: Armen Jalilyan
- Klasse: 2 AHITS
- Datum: 20.05.2026
- Fach: ITS1 Gruppe 1
- Thema: wc / cut / sed Übungen

wc ... word count
cut... filtert Spalten
sed ... stream editor

## Übung(wc):
- Aufgabenstellung:
Recherchiere, experimentiere und erkläre die folgenden Optionen:
- m: Zählt Zeichen (characters).
- Beispiel für -m:
```sh
echo "Hallo" | wc -m
```
- Ergebnis:
```sh
6
```
- Hallo hat ja 5 Buchstaben aber wegen dem \n vom echoo ist das Ergebnis 6, dass \n wird also dazugezählt.
- l: Zählt Zeilen.
- Beispiel für -l:
```sh
printf "a\nb\nc\n" | wc -l
```
- Ergebnis:
```sh
3
```
- L: Gibt die Länge der längsten Zeile aus.
- Beispiel für - L:
```sh
wc -L klassenkassa.csv
```
- Ergebnis:
```sh
38 klassenkassa.csv
```
- w: Zählt die Wörter.
- Beispiel für -w:
```sh
echo "eins zwei drei" | wc -w
```
- Ergebnis:
```sh
3
```
- $ echo eins zwei drei | wc - klassenkassa.csv
- Erklärung:
- echo eins zwei drei erzeugt die Ausgabe
- Die Pipe (|) leitet die Ausgabe des linken Befehls an den rechten weiter, also der Text wird an wc geschickt.
- wc zählt die Zeilen, Wörter und Bytes
- Das - liest zusätzlich von der Standardeingabe, also den text von echo
- wc zählt auch die Datei klassenkassa.csv

# Cut und Grep

## Übung (cut)
- Aufgabenstellung:
- Recherchiere und experimentiere – cut:

- in welchen Varianten können Felder mit der -f Option angegeben werden?
- was unterscheidet die -c Option von -f?

- Lösung:

Ein einzelnes Feld:
```sh
cut -d "," -f 1 klassenkassa.csv
```
Mehrere Felder:
```sh
cut -d "," -f 1,3 klassenkassa.csv
```
```sh
cut -d "," -f 2-4 klassenkassa.csv
```
- Unterschied zwischen -c und -f

Zeichen auswählen:
```sh
echo "ABCDEFGHIJ" | cut -c 1-5
```
Ausgabe:
```sh
ABCDE
```

Felder auswählen:
```sh
cut -d "," -f 2 klassenkassa.csv
```

## Übung (Anzahl Werkstatt)
- Aufgabenstellung:
Schreibe ein shell Kommando (Einzeiler) das in klassenkassa.csv die Anzahl der Einträge mit dem Text Werkstatt zählt.

- Lösung:
```sh
grep "Werkstatt" klassenkassa.csv | wc -l
```

## Übung (Werkstatt Summe)
- Aufgabenstellung:
Schreibe ein shell Kommando (Einzeiler) das in klassenkassa.csv alle Beträge mit dem Text Werkstatt in folgender Form ausgibt:
```sh
3.0+15.0+3.0+13.0+2.5
```
- Lösung:
```sh
grep "Werkstatt" klassenkassa.csv | cut -d "," -f 3 | paste -sd+
```
- Ausgabe Beispiel:
```sh
3.0+15.0+3.0+13.0+2.5
```

## Übung (HTML nach Markdown Konverter)
- Aufgabenstellung:
Verwende sed Kommandos um (einfache) HTML Dokumente nach Markdown zu konvertieren. Nimm folgendes HTML Dokument als Beispiel
```sh
<!DOCTYPE html>
<html>
<body>

<h1>Heading 1</h1>

<p> Franz jagt im komplett <b>verwahrlosten Taxi</b> quer durch Bayern.
Zwölf Boxkämpfer jagen Viktor quer über den großen Sylter Deich.
</p>

<h2>Heading 2</h2>

<p>
Bei jedem klugen Wort von <i>Sokrates</i> rief Xanthippe zynisch: Quatsch!
Typisch fiese Kater würden Vögel bloß zum Jux quälen.
</p>

<p>Programming <code>code</code> example:</p>

</body>
</html>
```
- Lösungen:

Überschrift H1:
```sh
sed 's/<h1>\(.*\)<\/h1>/# \1/"
```
HTML:
```sh
<h1> Heading 1</h1>
```
Markdown:
```sh
# Heading 1
```
Fett:
```sh
seds/<b>\(*\)<\/b>/**\1**/"
```
HTML:
```sh
<b>verwahrlosten Taxi</b>
```
Markdown:
```sh
**verwahrlosten Taxi**
```
Kursiv:
```sh
sed 's/<i>\(.*\)<\/i>/*\1*/'
```
HTML:
```sh
<i>Sokrates</i>
```
Markdown:
```sh
*Sokrates*
```
Code:
```sh
sed 's/<code>\(.*\)<\/code>/°\1'/'
```
HTML:
```sh
<code>code</code>
```
Markdown:
```sh
`code`
```
H2:
```sh
sed 's/<h2>\(.*\)<\/h2>/## \1/'
```
HTML:
```sh
<h2>Heading 2</h2>
```
Markdown:
```sh
## Heading 2
```

## Übung (sed Tutorial)
- AUfgabenstellung:
Lies Sed Command in Linux/Unix with examples und probiere alle gezeigten Möglichkeiten an einem eigenen Beispiel (z.B. mit klassenkassa.csv) aus.

- Lösungen:

Text ersetzen:
```sh
sed 's/Werkstatt/Labor/"
```
Alle Vorkommen ersetzen:
```sh
sed 's//;/g'
```
Zeile anzeigen:
```sh
sed -n "2p"
```
Zeile löschen:
```sh
sed '3d'
```

## Challenge (HMTL nach Markdown)
- Aufgabenstellung:
Schreibe sed Kommandos um folgende HTML Syntax nach Markdown zu wandeln:
```sh
<p>Programming <code>code</code> example:</p>

<ul>
  <li>C++</li>
  <li>Python</li>
  <li>Java</li>
</ul>

  <p>
    Link zur <a href="https://htl-braunau.at">HTL</a> Braunau.
  </p>

  <img src="htl_logo.jpg">
```
- Lösungen:
Code im Text:
```sh
sed s/<code>\(.*\)<\/code›/ \1'/"
```
HTML:
```sh
<code>code</code>
```
Markdown:
```sh
`code`
```
Liste:
```sh
sed 's/<li>\(.*\)<\/1i>/- \1/'
```
HTML:
```sh
<li>Python</li>
```
Markdown:
```sh
- Python
```
Link:
```sh
sed s#<a href=\[^"]*\)>\(*\)</a>#[\2](\1)#'
```
HTML:
```sh
<a href="https://htl-braunau.at">HTL</a>
```
Markdown:
```sh
[HTL](https://htl-braunau.at)
```
Bild:
```sh
sed 's#<img src="\([^"]*\)"›#! [](\1)#'
```
HTML:
```sh
<img src="htl_logo.jpg">
```
Markdown:
```sh
![](htl_logo.jpg)
```